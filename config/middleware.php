<?php

declare(strict_types=1);

use Slim\App;

return function (App $app) {
    // Add your middleware here.
    //$app->add(TrailingSlash::class);

    // Add Guard as middleware
    //$app->add(Guard::class);
};
